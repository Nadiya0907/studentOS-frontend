import { NavLink, Outlet } from 'react-router-dom';
import { LayoutDashboard, Users, FileText, BarChart3, MessageSquare, LogOut } from 'lucide-react';
import { useAuth } from '../../context/AuthContext';
const links=[['/admin','Dashboard',LayoutDashboard],['/admin/users','Users',Users],['/admin/reports','Reports',FileText],['/admin/analytics','Analytics',BarChart3],['/admin/feedback','Feedback',MessageSquare]];
export default function AdminLayout(){const {logout}=useAuth();return <div className="min-h-screen bg-bg text-gray-200 md:flex"><aside className="hidden w-64 border-r border-bg-border bg-bg-sidebar p-5 md:flex md:flex-col"><div className="mb-8 text-lg font-bold text-white">StudentOS Admin</div><nav className="space-y-1">{links.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/admin'} className={({isActive})=>`flex items-center gap-3 rounded-xl px-3 py-2 text-sm ${isActive?'bg-bg-hover text-white':'text-gray-400 hover:bg-bg-hover hover:text-white'}`}><Icon size={17}/>{label}</NavLink>)}</nav><button onClick={logout} className="mt-auto flex items-center gap-2 rounded-xl px-3 py-2 text-sm text-gray-400 hover:bg-bg-hover hover:text-white"><LogOut size={17}/>Logout</button></aside><main className="min-w-0 flex-1 p-5 md:p-8"><Outlet/></main></div>}
